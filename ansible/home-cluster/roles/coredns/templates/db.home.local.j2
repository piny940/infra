$TTL 86400
$ORIGIN home.local.
@   IN  SOA ns.home.local. admin.home.local. (
        2025091402 ; serial
        3600       ; refresh (1 hour)
        600        ; retry (10 minutes)
        604800     ; expire (1 week)
        86400      ; minimum (1 day)
    )
    IN  NS  ns.home.local.
ns  IN  A   {{ coredns.host }}
procyon IN A {{ procyon.host }}
