---
- name: Install yadm
  ansible.builtin.apt:
    update_cache: true
    name:
      - yadm
    state: latest
- name: Yadm clone
  ansible.builtin.command:
    cmd: yadm clone https://github.com/piny940/dotfiles.git
    creates: README.md
  become: false
