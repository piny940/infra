---
all:
  hosts:
    pollux:
      ansible_host: "{{ pollux.host }}"
      ansible_ssh_private_key_file: ~/.ssh/ed25519
      ansible_user: ansai
      ansible_become_password: "{{ server.password }}"
      ansible_python_interpreter: /usr/bin/python3
    deneb:
      ansible_host: "{{ deneb.host }}"
      ansible_ssh_private_key_file: ~/.ssh/ed25519
      ansible_user: ansai
      ansible_become_password: "{{ server.password }}"
      ansible_python_interpreter: /usr/bin/python3
    bellatrix:
      ansible_host: "{{ bellatrix.host }}"
      ansible_ssh_private_key_file: ~/.ssh/ed25519
      ansible_user: ansai
      ansible_become_password: "{{ server.password }}"
      ansible_python_interpreter: /usr/bin/python3
    castor:
      ansible_host: "{{ castor.host }}"
      ansible_ssh_private_key_file: ~/.ssh/ed25519
      ansible_user: ansai
      ansible_become_password: "{{ server.password }}"
      ansible_python_interpreter: /usr/bin/python3
    altair:
      ansible_host: "{{ altair.host }}"
      ansible_ssh_private_key_file: ~/.ssh/ed25519
      ansible_user: ansai
      ansible_become_password: "{{ server.password }}"
      ansible_python_interpreter: /usr/bin/python3
    betelgeuse:
      ansible_host: "{{ betelgeuse.host }}"
      ansible_ssh_private_key_file: ~/.ssh/ed25519
      ansible_user: ansai
      ansible_become_password: "{{ server.password }}"
      ansible_python_interpreter: /usr/bin/python3
    sirius:
      ansible_host: "{{ sirius.host }}"
      ansible_ssh_private_key_file: ~/.ssh/ed25519
      ansible_user: ansai
      ansible_become_password: "{{ server.password }}"
      ansible_python_interpreter: /usr/bin/python3
    rigel:
      ansible_host: "{{ rigel.host }}"
      ansible_ssh_private_key_file: ~/.ssh/ed25519
      ansible_user: ansai
      ansible_become_password: "{{ server.password }}"
      ansible_python_interpreter: /usr/bin/python3
    vega:
      ansible_host: "{{ vega.host }}"
      ansible_ssh_private_key_file: ~/.ssh/ed25519
      ansible_user: ansai
      ansible_become_password: "{{ server.password }}"
      ansible_python_interpreter: /usr/bin/python3
    olive:
      ansible_host: "{{ olive.host }}"
      ansible_ssh_private_key_file: ~/.ssh/ed25519
      ansible_user: ansai
      ansible_become_password: "{{ server.password }}"
      ansible_python_interpreter: /usr/bin/python3
  children:
    k0sctl:
      hosts:
        sirius:
    master_nodes:
      hosts:
        altair:
        vega:
        deneb:
    worker_nodes:
      hosts:
        betelgeuse:
        rigel:
        bellatrix:
    ha_proxies:
      hosts:
        castor:
        pollux:
