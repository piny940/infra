---
- name: Setup Master Node
  hosts: master_node
  become: true
  roles:
    - containerd
    - pre_install
    - helm
    - kubeadm
    - cli
  vars_files:
    - vars/hosts.yaml
    - vars/kubernetes.yaml
