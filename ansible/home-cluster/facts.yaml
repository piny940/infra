---
- name: Gather Facts
  hosts: all
  gather_facts: true
  vars_files:
    - vars/secrets.yaml
    - vars/kubernetes.yaml
  tasks:
    - name: Display Gathered Facts
      ansible.builtin.debug:
        var: ansible_facts
    - name: Show all variables
      ansible.builtin.debug:
        var: hostvars
