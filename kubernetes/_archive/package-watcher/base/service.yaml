apiVersion: v1
kind: Service
metadata:
  name: package-watcher
spec:
  selector:
    app: package-watcher
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
