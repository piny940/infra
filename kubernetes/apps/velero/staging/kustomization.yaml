apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: velero
namePrefix: stg-
resources:
  - ../base
labels:
  - pairs:
      env: staging
    includeSelectors: true
# configMapGenerator:
#   - name: velero-conf
#     behavior: merge
#     literals: []
