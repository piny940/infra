apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm.yaml
  - external-secret.ayml
configMapGenerator:
  - name: nextcloud-conf
    literals:
      - NEXTCLOUD_HOST=
      - STORAGE_SIZE=
