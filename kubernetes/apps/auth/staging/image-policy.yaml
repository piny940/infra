apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: auth
spec:
  imageRepositoryRef:
    name: auth
  filterTags:
    pattern: "^stg-[a-z0-9]+-(?P<seq>[0-9]+)$"
    extract: "$seq"
  policy:
    numerical:
      order: asc
