apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: package-watcher
spec:
  imageRepositoryRef:
    name: package-watcher
  filterTags:
    pattern: "^main-[a-z0-9]+-(?P<n>[0-9]+)$"
    extract: "$n"
  policy:
    numerical:
      order: asc
