apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: package-watcher
namePrefix: stg-
resources:
  - ../base
labels:
  - pairs:
      env: staging
    includeSelectors: true
# configMapGenerator:
#   - name: package-watcher-conf
#     behavior: merge
#     literals: []
