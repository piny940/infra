apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
namePrefix: stg-
resources:
  - ../base
  - image-policy.yaml
  - ingress.yaml
labels:
  - pairs:
      env: staging
    includeSelectors: true
# configMapGenerator:
#   - name: package-watcher-conf
#     behavior: merge
#     literals: []
