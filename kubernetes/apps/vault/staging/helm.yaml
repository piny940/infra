apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vault
  namespace: vault
spec:
  chart:
    spec:
      version: 0.27.0
  test:
    enable: true
  values:
    server:
      ingress:
        hosts:
          - host: stg-vault.piny940.com
