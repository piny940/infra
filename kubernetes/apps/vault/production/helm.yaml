apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vault
  namespace: vault
spec:
  chart:
    spec:
      version: 0.27.0
  values:
    server:
      ingress:
        hosts:
          - host: vault.piny940.com
