apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: portfolio-secret
spec:
  secretStoreRef:
    name: vault-secret-store
    kind: SecretStore
  refreshInterval: 1m
  data:
    - secretKey: test-username
      remoteRef:
        key: config
        property: username
