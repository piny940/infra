apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-backend
spec:
  template:
    spec:
      containers:
        - name: app
          env:
            - name: DB_NAME
              value: portfolio_staging
