apiVersion: v1
kind: Service
metadata:
  name: portfolio-admin-staging
spec:
  selector:
    app: portfolio-admin-staging
  ports:
    - name: http
      protocol: TCP
      port: 4400
      targetPort: 4400
  type: ClusterIP
