apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-admin-staging
  labels:
    app: portfolio-admin-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio-admin-staging
  template:
    metadata:
      labels:
        app: portfolio-admin-staging
    spec:
      containers:
        - name: app
          image: ghcr.io/piny940/portfolio-admin:main-2ed09ddc7afd3afbe01d9a9d47f7b99fd27c43a8-58 # {"$imagepolicy": "default:portfolio-admin-staging"}
          ports:
            - containerPort: 4400
