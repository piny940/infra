apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-admin-frontend-staging
  labels:
    app: portfolio-admin-frontend-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio-admin-frontend-staging
  template:
    metadata:
      labels:
        app: portfolio-admin-frontend-staging
    spec:
      containers:
        - name: app
          image: ghcr.io/piny940/portfolio-admin-frontend:main-7fca047663d3dbe5d26f93af5e4be3f4484d07d2-32 # {"$imagepolicy": "default:portfolio-admin-frontend-staging"}
          ports:
            - containerPort: 4400
