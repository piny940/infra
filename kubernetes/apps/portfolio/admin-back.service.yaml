apiVersion: v1
kind: Service
metadata:
  name: portfolio-admin-backend
spec:
  selector:
    app: portfolio-admin-backend
  ports:
    - name: http
      protocol: TCP
      port: 4400
      targetPort: 4400
  type: ClusterIP
