apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
namePrefix: stg-
commonLabels:
  env: staging
resources:
  - ../base
  - ingress.yaml
patches:
  - target:
      kind: HelmRelease
      name: grafana
    path: helm.yaml
