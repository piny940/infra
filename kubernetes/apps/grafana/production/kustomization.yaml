apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
commonLabels:
  env: production
resources:
  - ../base
  - ingress.yaml
patches:
  - target:
      kind: HelmRelease
      name: grafana
    path: helm.yaml
