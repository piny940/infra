apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
labels:
  - includeSelectors: true
    pairs:
      env: staging
namePrefix: stg-
images:
  - name: flux-target-updater
    newName: ghcr.io/piny940/flux-target-updater # {"$imagepolicy": "default:stg-flux-target-updater:name"}
    newTag: main-7b2bb145d5ffd9d6e2554eb2c550eccd0c9f35ac-20250212224831 # {"$imagepolicy": "default:stg-flux-target-updater:tag"}
resources:
  - ../base
  - deployment.yaml
  - external-secret.yaml
  - image-policy.yaml
  - image-repository.yaml
  - ingress.yaml
  - service-account.yaml
  - service.yaml
configurations:
  - kustomizeconfig.yaml
configMapGenerator:
  - name: flux-target-updater-conf
    behavior: merge
    literals:
      - APP_DEBUG=false
      - APP_PORT=8080
