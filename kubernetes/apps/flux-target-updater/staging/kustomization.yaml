apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
labels:
  - includeSelectors: true
    pairs:
      env: staging
namePrefix: stg-
images:
  - name: flux-target-updater
    newName: ghcr.io/piny940/flux-target-updater # {"$imagepolicy": "default:stg-flux-target-updater:name"}
    newTag: main-c7cb972a61d84715728aa20bdf816dc859d69f49-20241015053930 # {"$imagepolicy": "default:stg-flux-target-updater:tag"}
resources:
  - ../base
  - deployment.yaml
  - external-secret.yaml
  - image-policy.yaml
  - image-repository.yaml
  - ingress.yaml
  - service-account.yaml
  - service.yaml
configurations:
  - kustomizeconfig.yaml
configMapGenerator:
  - name: flux-target-updater-conf
    behavior: merge
    literals:
      - APP_DEBUG=false
      - APP_PORT=8080
