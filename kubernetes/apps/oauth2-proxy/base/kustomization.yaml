apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: oauth2-proxy
resources:
  - deployment.yaml
  - external-secret.yaml
  - image-policy.yaml
  - service.yaml
configMapGenerator:
  - name: oauth2-proxy-conf
    literals:
      - ALLOWED_USERS=piny940
      - CLIENT_ID=
      - SERVER_URL=
configurations:
  - kustomizeconfig.yaml
