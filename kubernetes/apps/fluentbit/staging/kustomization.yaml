apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
namePrefix: stg-
resources:
  - ../base
labels:
  - pairs:
      env: staging
    includeSelectors: true
# configMapGenerator:
#   - name: fluentbit-conf
#     behavior: merge
#     literals: []
