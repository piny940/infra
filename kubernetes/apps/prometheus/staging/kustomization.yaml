apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
nameSuffix: -staging
commonLabels:
  env: staging
resources:
  - ../base
patches:
  - target:
      kind: HelmRelease
      name: grafana
    path: helm.yaml
