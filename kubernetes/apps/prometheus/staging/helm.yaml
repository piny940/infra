apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
spec:
  chart:
    spec:
      version: 58.x
  test:
    enable: true
  values:
    server:
      baseURL: stg-alertmanager.piny940.com
