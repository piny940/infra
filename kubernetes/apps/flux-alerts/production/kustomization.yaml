apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  env: production
resources:
  - ../base
configMapGenerator:
  - name: flux-alerts-conf
    behavior: merge
    literals:
      - ENV=production
