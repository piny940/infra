apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-secret.yaml
  - provider.yaml
  - slack-alert.yaml
configMapGenerator:
  - name: flux-alerts-conf
    literals:
      - ENV=
