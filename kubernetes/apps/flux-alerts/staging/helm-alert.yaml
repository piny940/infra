apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: helm-alert
  namespace: flux-system
spec:
  eventMetadata:
    env: staging
  eventSources:
    - kind: HelmRelease
      name: "*"
      namespace: monitoring
