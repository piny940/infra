apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
namePrefix: stg-
commonLabels:
  env: staging
resources:
  - ../base
configMapGenerator:
  - name: flux-alerts-conf
    behavior: merge
    literals:
      - ENV=staging
