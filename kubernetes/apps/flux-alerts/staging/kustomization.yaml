apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: stg-
commonLabels:
  env: staging
resources:
  - ../base
  - github-token.yaml
  - githubdispatch.yaml
  - helm-alert.yaml
  - helm-promotion.yaml
patches:
  - path: slack-alert.yaml
