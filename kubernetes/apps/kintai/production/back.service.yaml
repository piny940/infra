apiVersion: v1
kind: Service
metadata:
  name: kintai-backend
spec:
  selector:
    app: kintai-backend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
