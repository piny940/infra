apiVersion: v1
kind: Service
metadata:
  name: kintai-frontend
spec:
  selector:
    app: kintai-frontend
    env: production
  ports:
    - name: http
      protocol: TCP
      port: 4400
      targetPort: 4400
  type: ClusterIP
