#@ load("@ytt:library", "library")
#@ load("@ytt:template", "template")

#@ def values():
name: kube-dashboard-oauth2-proxy
serverFQDN: kube-dashboard.piny940.com
upstreamURL: http://kubernetes-dashboard.kubernetes-dashboard.svc.cluster.local:443
secretKey: kube-dashboard
env: production
#@ end

#@ oauth = library.get("oauth2-proxy").with_data_values(values())
--- #@ template.replace(oauth.eval())
