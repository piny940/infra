apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-dashboard
resources:
  - ../base
labels:
  - pairs:
      env: production
    includeSelectors: true
# configMapGenerator:
#   - name: kube-dashboard-conf
#     behavior: merge
#     literals: []
