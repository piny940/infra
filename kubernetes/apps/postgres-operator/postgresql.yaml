apiVersion: "acid.zalan.do/v1"
kind: postgresql
metadata:
  name: cluster-db
spec:
  teamId: cluster
  volume:
    size: 10Gi
  numberOfInstances: 1
  users:
    kintai:
      - createdb
  databases:
    kintai_production: kintai
  postgresql:
    version: "15"