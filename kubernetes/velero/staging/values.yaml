configuration:
  backupStorageLocation:
    - name: default
      provider: velero.io/gcp
      bucket: stg-velero-backup.piny940.com
      validationFrequency: 3m
      credential:
        name: google-credentials
        key: gcp
schedules:
  backup-daily:
    schedule: "48 3 * * 0"
