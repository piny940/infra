configuration:
  backupStorageLocation:
    - name: default
      provider: aws
      bucket: stg-velero-backup-piny940-com
      credential:
        name: velero-backblaze-credential
        key: backblaze
schedules:
  backup-daily:
    schedule: "48 3 * * 0"
