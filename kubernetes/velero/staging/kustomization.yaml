apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: velero
namePrefix: stg-
commonLabels:
  env: staging
resources:
  - ../base
configMapGenerator:
  - name: velero-workload-identity-credential-configuration
    behavior: merge
    files:
      - gcp-credential-configuration.json
